pluginManagement {
    repositories {
        maven {
            name = 'Fabric'
            url = 'https://maven.fabricmc.net/'
        }
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "root"

enableFeaturePreview("TYPESAFE_PROJECT_ACCESSORS")

include(":lib")
//include(":mod") // TODO: gradle is being ~~unusually~~ weirdly pissy about this
